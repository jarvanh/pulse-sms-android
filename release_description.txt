v1.14.1 adds a ton of smaller tweaks: dismiss notifications when you delete and archive messages, improves the login process, improves the system-level private notifications, the app doesn't forcefully jump to the bottom when new messages arrive, various bug/crash fixes, and paves the way for some major changes in the future!