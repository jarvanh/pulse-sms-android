This release adds a new "MMS Configuration" section that can be accessed from the "Global Settings" three dot overflow. It also improves the styling of YouTube previews in conversations and fixes some bugs around rotating sent images.